import 'package:flutter/material.dart';

class FindHousePage extends StatelessWidget {
  const FindHousePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('找房'),
      ),
    );
  }
} 